# 그리디 알고리즘, 거스름 돈 

# 동전으로 반환할 금액 설정
n = 1260 
# 동전의 개수 초기화
cnt = 0 

# 동전별 값을 저장
coins = [500, 100, 50, 10]

# 동전의 종류만큼 반복문을 시작
for coin in coins:
    # 동전으로 나눌수 있을만큼 나눈후 그 나눈 횟수만큼 동전의 개수 증가
    cnt += n // coin
    # 동전으로 나눈후 나머지를 n 으로 저장
    n %= coin

# 결과 출력
print(cnt)
